node_modules/
dist/
.env
*.log
.next/
next-env.d.ts

# v1 data — only ignore raw snapshots and cache (canonical + derived are committed)
data_v1/l1_facts/raw/
data_v1/cache/

# Backfill raw API responses and state (large, re-fetchable)
data/backfill/
